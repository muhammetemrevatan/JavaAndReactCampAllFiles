package core;

import JGoogle.GoogleAdapter;
import entities.concretes.User;

public class JGoogleManagerAdapter implements CheckAdapter {
	
	@Override
	public void register(User user) {
		GoogleAdapter googleAdapter = new GoogleAdapter();
		googleAdapter.googleRegister();
		
	}

}
